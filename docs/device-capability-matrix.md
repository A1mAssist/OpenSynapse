# OpenSynapse Device Capability Matrix

Date: 2026-08-18

This matrix separates Synapse-visible capability, protocol evidence, readback, and current implementation state. It covers only Blade 16 2025 (`1532:02C6`) and Viper V3 HyperSpeed (`1532:00B8`). Account, cloud, store, news, game-library, telemetry, and firmware-update features are excluded.

Status vocabulary:

- **Verified**: current-device read/write/readback/restore evidence is complete; a production write may be exposed.
- **SourceBacked**: exact public or device-specific evidence exists, but current-device write/readback/restore evidence is incomplete; production writes remain disabled.
- **Approximate**: an evidence-based software implementation is production-connected, but exact native parameters, mapping, timing, or visual parity remain unproven; it must not be described as 1:1.
- **Native**: supplied by a documented Windows API or system setting rather than a Razer private command.
- **Blocked**: the feature is known or claimed to exist, but current evidence is insufficient for a runnable command.
- **Explicitly excluded**: outside this release or unsupported by the target device.

## Blade 16 2025 (`02C6`)

| Synapse/device function | Parameters and boundaries | Exact protocol evidence | Readback | Development state |
|---|---|---|---|---|
| Keyboard brightness | Raw `0..255`, UI `0..100%` | OpenRazer PR `#2450`; class `0E`, GET `84`, SET `04`, args `[01,value]` | Yes | Verified |
| Performance mode | Product-selectable firmware values: Balanced `0`, Performance `2`, Battery Saver `3`, Custom `4`, Silent `5`, HyperBoost `7`; `6` is automatic Balanced DC readback, not the Battery Saver UI mode | Current Product 710 enum/converter plus `blauzim/razer-ctl` exact `RZ09-05286` / `02C6`; class `0D`, GET `82`, SET `02`, two zones. Battery Saver reuses this path after BIOS `2.02`, EC `1.09`, and model-specific VBIOS gates | Yes, both zones must agree; current hardware changed one mode and restored it while preserving fan mode. Mode `3` and automatic mode `6` still need targeted physical confirmation | `Verified` transaction and recovery; production UI no longer mislabels mode `6`. Battery Saver remains source-backed until its firmware gate and mode `3` are physically read/write/readback/restored |
| Fan mode and stored target | Automatic `0`; Manual `1`; verified `02C6` safe target range `2000..5000 RPM`, step `100` | Exact `02C6` public implementations; GET `0D82` and `0D81`, SET `0D01`, two zones; independent `razer-light` driver confirms `0D01` args `[00,zone,rpm/100]` | Stored target is read back; current tachometer RPM uses Product 710 GET `0D88` for CPU/GPU and returned `2200/2000 RPM` on 2026-08-13. On 2026-08-17 a hardware transaction changed `Automatic / 3800 RPM` to `Manual / 3900 RPM`, read it back, produced an audible fan change, and restored `Automatic / 3800 RPM` | Read-only telemetry now includes current CPU/GPU RPM; production manual writes remain `Blocked` pending process-exit, device-disconnect, and sleep/wake recovery |
| Smart fan curve | Product nodes contain `temperature`, `cpuFanSpeedValue`, and `gpuFanSpeedValue`; CPU/GPU arrays are strictly increasing after validation; temperature source is CPU, GPU, or both; current profile uses 6..10 nodes, 40/30 C low-temperature baselines, and 2300 RPM baseline | Local Product 710 module stores `smartFanCurve.cpu/gpu`, selects the active sensor (or the whole candidate with the higher CPU output when both are enabled), linearly interpolates both fan outputs, then calls `setThermalFanSpeed` independently for CPU/GPU. There is no separate firmware curve-table report | Every target change uses the two-target GET/write/readback transaction; concurrent Stop/Dispose is serialized, caller cancellation cannot cancel restoration, and stop/cancel/failure restores both stored targets and the original mode. Tests cover transient sensor loss and the bounded terminal failure path. Curve values use the observed `Math.floor(value/100)` write rule | `SourceBacked`; typed Product-710 curves, interpolation, independent targets, bounded sensor-loss handling, and an owned software runtime are implemented. Production UI remains disabled until disconnect, sleep/resume and hard-exit safety are physically validated |
| CPU boost in Custom mode | Low `0`, Medium `1`, High `2`, Boost `3`, Undervolt preset `4` | Exact `02C6` implementation; GET `0D87`, SET `0D07`, CPU cluster `1` | Yes; current hardware passed same-value and one-step change, then restored CPU/GPU and performance mode | `Verified`; production SET requires current Custom mode and preserves the GPU cluster |
| GPU boost in Custom mode | Low `0`, Medium `1`, High `2` | Exact `02C6` implementation; GET `0D87`, SET `0D07`, GPU cluster `2` | Yes; current hardware passed same-value and one-step change, then restored CPU/GPU and performance mode | `Verified`; production SET requires current Custom mode and preserves the CPU cluster |
| Max fan speed mode | Disable `0`, Enable `2`; production write requires Custom performance mode | Exact `02C6` implementation; GET `078F`, SET `070F` | Yes; GET has a known remaining-packet exception; current hardware passed toggle/readback/restore and restored the original performance mode | `Verified`; production SET enabled after a successful current-path GET, with readback and failure recovery |
| Battery charge limit | `50,55,60,65,70,75,80%`; disabled=`100%` | Exact `02C6` implementations; GET `0792`, SET `0712`; raw `B2,B7,BC,C1,C6,CB,D0,50` | Yes; current hardware changed one allowed value and restored it; GET has a known remaining-packet exception | `Verified`; production SET enabled with readback and failure recovery |
| System battery and lighting sleep policy | Windows battery status plus plugged/battery brightness and host idle/display-off rules | Product 710 advertises `readSystemBattery=true`; its `battery` state machine is `powerOff`. `switchOffLighting` is a host policy that eventually calls the verified keyboard-brightness path | Windows/system state is authoritative; the shared `02/07/80,84,88,83` reports returned zero-valued ACKs and are not Product 710 capability evidence | `Native`; the misattributed Blade HID telemetry and UI fields were removed |
| Firmware quick effects | Off, wave, spectrum, reactive, static, breathing, starlight | OpenRazer `02C6`; class `03`, SET `0A`; Starlight deliberately declares `data_size=01` while carrying nine argument bytes, hardware-tested upstream in commit `4e65f1249587` | No standard effect GET; current `02C6` acknowledged Static-red and restore SETs without any visible keyboard change | `SourceBacked`; not used by the PID 710 production backend because Synapse registers `rzDevice25LedMatrixSkipSetEffect` and skips this command |
| Custom RGB matrix | `6 x 17`; row packet `[FF,row,start,stop,RGB...]` | Software mode plus effect-8 engine gate; physical A/B on this Product 710 requires every six class `03`, SET `0B` rows to be followed by class `03`, SET `0A`, args `[05,00]`; transaction IDs `1..7`, 1 ms delay; official layout maps 7 x 16 logical keys into 6 x 17 sparse device slots | 2026-08-15 A/B: rows then per-frame CustomFrame commit rendered the locator correctly; a single pre-stream commit produced only a solid color | `Verified`; production backend uses the physically confirmed seven-report frame, sparse-layout projection, bounded latest-frame pumping, failure-safe Normal-mode recovery, and persistent-frame restore |
| Software quick effects | Off, Static, Breathing, Spectrum, Wave, Fire, Reactive, Ripple, Audio Meter, Ambient, Wheel, Starlight, Tidal | Synapse registers the PID 710 Basic Lighting Engine at 25 FPS. Wave now uses the Product 710 `Width=100`, `Speed=25`, `Pause=0`, `Angle=90\|270` projection. Fire now uses the DLL's real 100-keyframe base field, `Rate=160` five-phase interpolation into a 500-frame loop, fixed source/propagation/color tables, and `7 x 23 -> 6 x 17` reverse-row crop. Wheel uses the recovered Product 710 center, direction and timing projection. Starlight implements the recovered scheduler/RNG/node lifecycle; Tidal implements the Product-default three-lane projection | 2026-08-15 physical runs confirmed Reactive/Ripple key response including right-side M3/M4/M5, Audio Meter playback/pause response, Ambient four-quadrant color mapping, clean Normal restoration, and Windows 11 borderless capture. On 2026-08-17 Starlight and Tidal both passed visual and clean Normal-restoration checks. Wave/Fire/Wheel still lack exact current-device visual comparison evidence | `Approximate`; production `IBladeLightingController` is App-owned. Wave/Fire/Wheel use recovered native algorithms but remain Approximate until physical comparison. Reactive/Ripple own their keyboard input adapter; Audio Meter owns recoverable WASAPI loopback; Ambient owns permission-gated internal-display Graphics Capture. Wheel, Starlight and two-color Tidal are production-visible. |
| Chroma Studio | Local lighting manifest says supported; matrix `6 x 17` | Local Razer module confirms capability, public matrix writes exist | No full-state readback | Not planned until a truthful local shadow-state/recovery design exists |
| Lid logo Off / Static | Off, static | OpenRazer exact Blade branch: state GET/SET `03/03/80` and `03/03/00`, mode GET/SET `03/03/82` and `03/03/02`, args `[01,04,value]`, transaction `FF`; Blade on/off uses LED state rather than effect | 2026-08-13: Static visibly lit the physical logo, electronic readback matched, and original Off state was restored | `Verified`; production control may use the exact mode-then-power sequence with readback and restoration |
| Lid logo Breathing | breathing | Product 710 Synapse task runner sends `03/03/02` Set LED Effect with `[00,04,02]`, transaction `00`, then `03/03/00` Set LED State with `[00,04,01]` | 2026-08-17 Product 710 sequence visibly produced sustained Breathing for 30 seconds; both commands ACKed and the original Static state was electronically restored and read back. The user skipped observing the final physical Static restoration | `Verified` protocol and target effect; keep the production SET hidden until physical restoration is observed |
| Device mode (not lights-always-on) | Normal `00`; Driver `03` disables native Fn media-key behavior | Exact `02C6` implementation; GET `0084`, SET `0004` | Yes | No user-facing control; keep Normal mode. Always-on lighting requires a separate keep-alive design and is not represented by `0004` |
| Fn media keys and mute indicators | Fn+F1/F2/F3 map to VolumeMute/Down/Up; M5 toggles microphone mute; F1/M5 show the matching state | Product 710 `mapping_engine.dll` filter-driver graph uses RazerKey `10/11` for HyperShift, extended media scans `E0 20/2E/30`, and RazerKey `D4` output `{type:"audio",id:"mic",mute:2}` | 2026-08-17 production validation: Fn+F1/F2/F3 system media keys worked, M5 toggled microphone mute, both red indicators followed Core Audio state, Wave continued concurrently, and normal tray exit restored both indicators and native media-key behavior | `Verified`; MappingEngine starts before Driver Mode and is stopped only after Normal mode is restored. General remapping, HyperShift customization, Snap Tap, crash, and disconnect behavior remain separately gated |
| Snap Tap | Product 710 stores one `{key1,key2,id}` pair plus `isEnabled`; the default pair is A/D | `ON_SET_SNAP_TAP` persists `{isEnabled,keyList}` and injects one `snaptapId` into both host-side MappingEngine inputs; the HyperShift toggle executes on key release. The generic `0221` firmware kill-switch command is gated by OBM capabilities that Product 710 does not declare | Last-input-priority arbitration, held-key restoration, output reference counting, session flush, and failure-safe synthetic-key release are implemented and unit-tested; there is no Product 710 firmware state to read back | `SourceBacked`; the owned runtime is code-complete for simulation, but real A/D and focus/session/crash safety still require hardware validation |
| Display refresh rate | Current internal panel is `2560 x 1600`; local enumeration returned `48,60,75,100,120,240 Hz` | `QueryDisplayConfig` identifies exactly one active `LVDS`/embedded DP/embedded UDI/`INTERNAL` target; `EnumDisplaySettingsExW` and `ChangeDisplaySettingsExW` operate only on its returned GDI source name | Yes; set path performs `CDS_TEST`, apply, same-source readback, and best-effort restore | `Native`; backend read/set/profile power override implemented. Writes fail closed for zero/multiple internal paths, clone topology, or unsupported rates |
| Internal display brightness keys | Product 710 MappingEngine outputs `display/driverBrightnessDown`, `display/driverBrightnessUp`, and `display/driverBrightnessStop` | Windows SDK `BrightnessOverride.GetDefaultForSystem` changes and persists the current user's system brightness without a vendor service or elevation | The Windows API reports support and save success; physical Fn+F7/F8 behavior is not yet observed | `Native`; production callback handler and bounded 10% step are implemented, pending one physical key check |
| Native display mode | `UHD=0`, `FHD=1`; Product 710 also reads SKU bits `dds=1`, `miniLedResolution=2`, `illegalBatterySupport=4` | Product 710 calls GET `0D/8E`, SET `0D/0E`, size `01`; SKU GET is `0D/8F`. Only the subsequent restart uses `RzSystemService` | 2026-08-18 production transaction changed `UHD -> FHD`, read back FHD, restored UHD and read back UHD | `Verified` electronically; UI exposure and restart remain hidden because the physical panel/restart effect was not observed |
| HDR state | Product 710 calls `GetBladeMonitorWindowsHDRMode` only | The generic Razer HDR SET is not Product 710-used evidence; Windows Advanced Color APIs can provide the same read state | Read-only equivalent available through documented Windows APIs | `Native` read candidate; no admitted Razer setter |
| Local dimming | Off/On; current Product 710 declares Blade series 11 | Current shared MW groups series `10/11/12`, branches away from the generic native HDR helper, and read-modify-writes bit 3 of Power Mode Control through GET `078F` / SET `070F`, preserving bits 0..2 | 2026-08-16 Power Mode GET returned raw mask `0x00` (`localDimming=false`); the production reader now decodes this bit from the existing Max Fan GET without another report. No current-device reversible Local Dimming run | `SourceBacked`; nullable read-only telemetry and sibling-bit-preserving builder tests are complete, but production SET remains disabled until visual/readback/restore validation |
| One-time full charge | Valid only while the charge limiter is enabled; after reaching full charge, return to the configured limit | Product 710 read-modify-writes bit 2 of the same GET `078F` / SET `070F` Power Mode Control mask and preserves sibling bits | 2026-08-18 production transaction changed `false -> true`, read back true, restored false and read back false | `Verified` electronically; the long-running firmware auto-clear behavior remains unobserved, so the UI remains hidden |
| ICC color profiles | Local logs reference Display P3, Adobe RGB, BT.709, sRGB, Native profiles | Windows color-management APIs; vendor `.icm` files are local installation assets | Yes for active profile | Native for documented Windows color-management APIs; asset behavior still needs definition |
| GPU mode / NVIDIA OC / AMD OC | This Blade 16 configuration is Optimus-only; no Dedicated GPU-only target is in scope | GPU MUX is a vendor/native path and no exact `02C6` HID contract is accepted | N/A | Explicitly excluded for this device; Windows/Razer owns Optimus mode |
| THX spatial audio, EQ, normalization, voice clarity | Product 710 targets the exact `rznb11` speaker/headphone endpoints and THX v4 properties `spatial_enabled`, `eq_*`, `volume_leveling_*`, and `voice_clarity_*` | Product call/FFI property contracts are recovered and retained only as reverse-engineering notes | N/A | `Explicitly excluded`; OpenSynapse will not ship controls or runtime integration for these proprietary DSP features |
| Gaming Mode state | `0..255` state byte; M3 toggles the state and its white indicator; Product profile also stores host suppression flags | Product 710 middleware calls `00/88` GET and `00/08` SET with data size `04`; parser exposes `gameMode`, `keyCover`, `lifted`. Windows/Alt suppression remains host-side MappingEngine policy | Current-device M3 toggle, firmware readback and matching indicator state were physically confirmed on 2026-08-17 | `Verified`; production MappingEngine callback performs guarded SET/readback and synchronizes the M3 indicator |
| Fn primary (`func`/`multi`) | `func=0`, `multi=1`; Product call uses class `0` | Product 710-loaded module `48967` resolves `02/06` SET, data size `02`, args `[classId,alternateState]`; no Fn GET exists; mappings are regenerated host-side after the SET | No current-device reversible write/readback gate yet | `SourceBacked`; strict builder/parser exists, no production SET |
| Speaker/microphone mute indicator sync | Core Audio render/capture mute state; targets `1=speaker`, `2=microphone`, state `0/1` | Product 710 enters Driver Mode and sends size `03`, class `18`, command `04`, args `[00,target,muted]` over one persistent HID session | 2026-08-17: isolated and production F1/M5 On/Off runs visibly worked; Wave continued behind both red-key overrides, unmute restored the dynamic frame, and tray exit restored Normal mode with both indicators off | `Verified`; production starts MappingEngine before Driver Mode and restores Normal before stopping it |
| Trackpad toggle | `toggle` / current enabled state | `bladeTrackpad` MappingEngine event calls Synapse's `getTouchPadEnableStatus` / `toggleTouchPadEnableStatus` system helper; no Razer HID report is sent | Windows helper owns state; user confirmed the production UI toggle on current hardware on 2026-08-17 | `Native`; `WindowsTouchpadController` is wired to the Blade page with registry-backed availability and state readback |
| Startup animation | Enabled/disabled; available only when current Blade firmware is at least `1.08.00` | Product 710 firmware gate calls GET size `01`, class `0F`, command `98`, args `[00]`; SET size `02`, class `0F`, command `18`, args `[profileId=00,disableAnimation=0\|1]` | 2026-08-18 production transaction changed `true -> false`, read back false, restored true and read back true | `Verified` electronically; visual behavior requires a reboot and remains outside the current automated evidence |
| OLED | No Product 710 OLED call | Generic OLED strings are not attributable to this Blade | Not applicable | `Excluded`; no fake control |
| Firmware update | BIOS/EC/VBIOS manifests exist | Vendor updater only | Not applicable | Explicitly excluded |

## Viper V3 HyperSpeed (`00B8`)

| Synapse/device function | Parameters and boundaries | Exact protocol evidence | Readback | Development state |
|---|---|---|---|---|
| Current DPI | `100..30000`, step `50`; X/Y axes | OpenRazer PR `#2149`, tested on `00B8`; class `04`, GET `85`, SET `05` | Yes | Verified |
| DPI stages | Up to 5; default `400,800,1600,3200,6400`; max `30000`; X/Y step `50` | Product 184 source-backed chain: transaction `1F`, GET `0486`, SET `0406`; persistent storage arg `[01]`; SET payload `[storage,active,count] + [zeroBasedId,X_BE,Y_BE,00,00]` | 2026-08-13 current-device GET returned active stage 3/5 and `400,800,1600,3200,6400`; non-active stage 1 changed to `450x450`, read back exactly, then original table restored and read back exactly | Verified; backend setter is current-path gated, compares the complete table, and attempts exact restoration on failure/cancellation. The WinUI device page exposes an editor that submits the complete table. |
| Polling rate | `125,500,1000 Hz`; raw `08,02,01` | OpenRazer PR `#2149`, tested on `00B8`; class `00`, GET `85`, SET `05` | Yes | Verified |
| Battery level | Raw `0..255`, displayed as percentage estimate | OpenRazer PR `#2149`, tested on `00B8`; class `07`, command `80` | Strict Product 184 parser now validates envelope, transaction, command and CRC | Verified read-only; AA device has no charging state |
| Low-battery threshold | Raw `0..255`; official decode is `floor(raw / 255 * 100)` | Exact `00B8` OpenRazer driver switch; Product 184 `rzDevice30` GET decodes the raw byte and SET encodes `ceil(percent / 100 * 255)` | 2026-08-13 current-device GET returned `0x4D`, decoded by the official rule as `30%` | SourceBacked; validated read-only telemetry, no UI write/SET until write/readback/restore |
| Idle timeout | `60..900 seconds` | Exact `00B8` OpenRazer implementation; GET `0783`, SET `0703` | Strict Product 184 parser now validates range and envelope | Verified |
| Battery chemistry | Alkaline `00`, rechargeable NiMH `01`, lithium `02` | Exact `00B8` Synapse captures: transaction `1F`, SET `0714`, size `01`; the device returned success and echoed each value | No GET/readback was captured or established | SourceBacked SET builder only; no production control until an exact GET or another deterministic restore path is proven |
| Button assignment | Device button IDs are `1,2,3,4,5,9,10,96`; each assignment is addressed by `[profileId,buttonId,mode]` | Product 184 `obmEngineMouse` uses transaction `1F`, GET `028C`, SET `020C`, declared maximum size `0x50`; SET payload begins `[profileId,buttonId,mode,functionId,functionDataSize,...functionData]` | 2026-08-15 button 5 HyperShift passed temporary `Off`, exact readback, Normal-layer isolation, original-value restore, and an independent-process 16-assignment read. 2026-08-17 button 5 Normal `KeyCode[00,04]` physically emitted/stopped `A`, preserved HyperShift, restored `ButtonCode[05]`, and passed an independent full read | `Verified` for `Off`, known mouse `ButtonCode` values `1,2,3,4,5,9,10`, and source-backed two-byte `KeyboardKey`. Production serializes GET/readback/sibling isolation/non-cancelable restore; remaining function families stay rejected. WinUI still exposes only Off/mouse actions pending a keyboard-key selector |
| HyperShift layer and onboard Profile | `mode=0` Normal, `mode=1` HyperShift; Product 184 has exactly one fixed Profile (`maxProfilesSupported=1`, `profileIdList=[1]`) | Both layers use the same `028C/020C` board-mapping protocol; generic active-Profile GET `0584` is rejected by this device. Assignment responses return mode byte `1` even for Normal, so Synapse and OpenSynapse select the layer from request context. The product UI's activator is function `12`, size `1`, data `[1]`; generic function `17/[89]` is not a Product 184 path | Temporary `Off` affected only button 5 HyperShift storage; Normal remained `ButtonCode[5]`, then HyperShift was restored to `ButtonCode[5]`. Post-process full read found no residual change | `Verified` board-layer addressing/isolation and one fixed Profile. The function `12/[1]` activator remains `SourceBacked` and production-rejected until its own reversible persistence run passes |
| Extended assignment functions | Source-backed Product 184 families are keyboard key, DPI, multimedia, DoubleClick, HyperShift, mouse Turbo and keyboard Turbo; Macro/Profile/Lighting/Power/Controller/RazerKey/Windows shortcuts are excluded | Current official `OBMSpecs.supportedMappings`, product predicate and generic encoder close the exact Product 184 function/payload layouts | Keyboard A and DoubleClick completed physical write/readback/isolation/restore on 2026-08-17. DPI `[06]`, media Play/Pause `[CD,00]`, HyperShift `[01]`, keyboard Turbo A/100 ms `[00,04,64,00]`, and mouse Turbo button 1/100 ms `[01,64,00]` completed target readback, sibling-layer isolation, and original-value restoration on 2026-08-18 | `Verified` and production-enabled for every listed Product 184 family. DPI/media/HyperShift/Turbo have electronic protocol evidence; their physical trigger behavior is not claimed |
| Surface calibration | Generic mouse modules may contain the page, but exact local `00B8` support has not been proven | None accepted | Unknown | Do not expose based on generic bundle strings alone |
| Chroma lighting | Local lighting manifest explicitly sets Chroma and Chroma Studio support to false | Device has no supported lighting surface | Not applicable | Explicitly excluded |
| Firmware update | Vendor package path only | Vendor updater only | Not applicable | Explicitly excluded |

## Front-End / Back-End Boundary

The WinUI front end binds immutable telemetry and capability state. It never opens HID handles. A write control exists only for a `Verified` capability and remains disabled until its backend GET succeeds for the current device path; errors preserve `--` instead of inventing values.

The Profile page currently exposes selection, create, clone, rename, delete, executable bind/unbind, JSON import/export, and current-user startup registration. These are application-management capabilities and do not change the evidence status of any hardware command.

The Windows backend owns SetupAPI enumeration, feature-report transport, packet validation, retries, range checks, write/readback, and disconnect recovery. Strict embedded manifests select supported VID/PIDs, HID collections, protocol families, transport waits, and admitted request headers; strongly typed C# family handlers still own dynamic encoding, parsing, current-path gates, readback, and restoration. Closed Synapse module names or UI strings are evidence that a feature exists, not permission to invent a packet.

## Evidence Notes

- OpenRazer commit `6820f9da169d354bc7e6e93a0aa8683a6bb75792`, PR `#2149`, and PR `#2450`.
- OpenRGB commit `684b74ca9e2e2bfe096f074e1e8a1f7e04db5255`.
- `blauzim/razer-ctl` (MIT), including the `RZ09-05286` descriptor and performance/fan/battery command implementation.
- `encomjp/razer-control-revived` (GPL-2.0), used as independent corroboration rather than copied implementation.
- Local Razer AppEngine manifests, product modules, and native logs. No serial numbers, account identifiers, or other personal data are included here.
